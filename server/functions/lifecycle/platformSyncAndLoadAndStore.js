/**
 * A function that syncs all the data from all the connected platform of a user and then loads and store them in the DB
 */
const platformSyncAndLoadAndStore = async () => {
	try {
	} catch (error) {
		throw error
	}
}

export default platformSyncAndLoadAndStore
